#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: any input integer
 * Return: the absolute value of a
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
